import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
  ingredients: {
    listStyle: 'disc',
  },
  ingredientsItem: {
    display: 'list-item',
    listStylePosition: 'inside',
  },
})

export { useStyles }

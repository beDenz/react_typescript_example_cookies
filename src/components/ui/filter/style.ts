import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
  root: {
    width: 56,
    height: 56,
    borderRadius: 28,
    border: 1,
    borderStyle: 'solid',
    borderColor: '#dddddd',
  },
})

export { useStyles }

import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
  container: {
    display: 'flex',
    maxWidth: `${(1084 * 100) / 1280}%`,
    paddingTop: 632,
  },
})

export { useStyles }
